# Journal inbox

Agent fragments awaiting merge into `../JOURNAL.md`. Parallel agents write here, never to
JOURNAL.md directly — concurrent appends to one file lose entries silently. The orchestrator
merges, renumbers if needed, and deletes the fragment.
