<?xml version="1.0" encoding="UTF-8"?>
<!--
  OpenDAW Windows Installer
  
  Build with: build-installer.ps1
  
  Variables expected:
    $(var.SourceDir)     - Directory containing staged files
    $(var.UIBuildDir)    - Directory containing OpenDAW.exe
    $(var.EngineBuildDir) - Directory containing daw_engine.dll
    $(var.VCRedistDir)   - Directory containing vc_redist.x64.exe
-->
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
  <Product Id="*" 
           Name="OpenDAW" 
           Version="$(var.ProductVersion)" 
           Manufacturer="OpenDAW Team" 
           UpgradeCode="12345678-1234-1234-1234-123456789012">
    
    <Package InstallerVersion="200" 
             Compressed="yes" 
             InstallScope="perMachine" 
             Description="OpenDAW - Open Source Digital Audio Workstation"
             Manufacturer="OpenDAW Team" />

    <MajorUpgrade DowngradeErrorMessage="A newer version of [ProductName] is already installed." />
    <MediaTemplate />

    <!-- Feature tree -->
    <Feature Id="ProductFeature" Title="OpenDAW" Level="1">
      <ComponentGroupRef Id="ProductComponents" />
      <ComponentGroupRef Id="VCRedistComponents" />
    </Feature>

    <!-- Installation directory -->
    <Directory Id="TARGETDIR" Name="SourceDir">
      <Directory Id="ProgramFiles64Folder">
        <Directory Id="INSTALLFOLDER" Name="OpenDAW">
          <Component Id="MainExecutable" Guid="*">
            <File Source="$(var.SourceDir)\OpenDAW.exe" KeyPath="yes">
              <Shortcut Id="StartMenuShortcut" 
                        Directory="ApplicationProgramsFolder" 
                        Name="OpenDAW" 
                        WorkingDirectory="INSTALLFOLDER"
                        Icon="OpenDAWIcon.exe"
                        Advertise="yes" />
              <Shortcut Id="DesktopShortcut" 
                        Directory="DesktopFolder" 
                        Name="OpenDAW" 
                        WorkingDirectory="INSTALLFOLDER"
                        Icon="OpenDAWIcon.exe"
                        Advertise="no" />
            </File>
          </Component>
          
          <!-- Rust engine DLL -->
          <Component Id="RustEngine" Guid="*">
            <File Source="$(var.SourceDir)\daw_engine.dll" />
          </Component>
          
          <!-- AI modules -->
          <Component Id="AIModules" Guid="*">
            <File Source="$(var.SourceDir)\ai_modules" />
          </Component>
          
          <!-- README and license -->
          <Component Id="Documentation" Guid="*">
            <File Source="$(var.SourceDir)\README.md" />
          </Component>
        </Directory>
      </Directory>

      <!-- Start Menu -->
      <Directory Id="ProgramMenuFolder">
        <Directory Id="ApplicationProgramsFolder" Name="OpenDAW" />
      </Directory>

      <!-- Desktop -->
      <Directory Id="DesktopFolder" />
    </Directory>

    <!-- File association for .opendaw files -->
    <Component Id="FileAssociation" Guid="*" Directory="TARGETDIR">
      <ProgId Id="OpenDAW.Project" Description="OpenDAW Project">
        <Extension Id="opendaw" ContentType="application/x-opendaw">
          <Verb Id="open" Command="Open" TargetFile="MainExecutable" Argument=""%1"" />
        </Extension>
      </ProgId>
    </Component>

    <!-- Visual C++ Redistributable -->
    <ComponentGroup Id="VCRedistComponents" Directory="TARGETDIR">
      <Component Id="VCRedist" Guid="*">
        <File Source="$(var.VCRedistDir)\vc_redist.x64.exe" />
      </Component>
    </ComponentGroup>

    <!-- Custom action to install VCRedist -->
    <CustomAction Id="InstallVCRedist" 
                  FileKey="VCRedist" 
                  ExeCommand="/install /quiet /norestart" 
                  Execute="deferred" 
                  Return="check" 
                  HideTarget="no" 
                  Impersonate="no" />

    <InstallExecuteSequence>
      <Custom Action="InstallVCRedist" Before="InstallFinalize">
        <![CDATA[NOT Installed]]>
      </Custom>
    </InstallExecuteSequence>

    <!-- Icon (extracted from main executable) -->
    <Icon Id="OpenDAWIcon.exe" SourceFile="$(var.SourceDir)\OpenDAW.exe" />

    <!-- UI -->
    <UIRef Id="WixUI_InstallDir" />
    <Property Id="WIXUI_INSTALLDIR" Value="INSTALLFOLDER" />
    
    <!-- License (optional - will use default if not found) -->
    <!-- <WixVariable Id="WixUILicenseRtf" Value="$(var.SourceDir)\LICENSE.rtf" /> -->
    
    <!-- Banner images (optional - will use defaults if not found) -->
    <!-- <WixVariable Id="WixUIBannerBmp" Value="$(var.SourceDir)\banner.bmp" /> -->
    <!-- <WixVariable Id="WixUIDialogBmp" Value="$(var.SourceDir)\dialog.bmp" /> -->

  </Product>
</Wix>
